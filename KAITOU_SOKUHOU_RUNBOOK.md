# 解答速報ページ 運用ランブック（/kaitou-sokuhou/）

作成：2026-08-12 ／ 対象URL：https://takken.mainichi-lab.com/kaitou-sokuhou/

---

## 1. 目的（なぜ試験前に作ったか）

宅建講座アフィリの本番商戦は**試験後（11月〜年初）の「不合格→来年リベンジ組」**。
自己採点で不合格を悟った瞬間が最も講座申込に近い。その検索流入（「宅建 解答速報」「宅建 自己採点」「宅建 合格ライン」）を取るには、
**10/18の試験日までにインデックスされ、順位が育っている必要がある**。だから2か月前に受け皿を公開した。

収益導線：解答速報ページ → ゾーン3（明らかに届かなかった）→ /courses/ 比較ページ → A8リンク。

---

## 2. 実装内容

### 新規ファイル
- `kaitou-sokuhou/index.html` … 本体（`/legal.css` を共用、courses/ と同じトーン）
- `kaitou-sokuhou/seitou.js` … 正答データ（2026-08-30追加。採点ツールの正答はここ1箇所。差し込み手順＝§4-B2）
- `~/Desktop/claude/tools/check_seitou.py` … **正答データの事前チェッカー**（2026-08-31追加・公開リポジトリの外に置いてある）。当日 `seitou.js` を編集したら push 前に必ず通す

### 変更ファイル
| ファイル | 変更点 |
|---|---|
| `index.html` | フッター2箇所に `/kaitou-sokuhou/` リンク追加 |
| `courses/index.html` | 「あわせて読みたい」に追加 |
| `blog/takken-dokugaku-benkyohou.html` | 「あわせて読みたい」に追加 |
| `blog/takken-benkyo-jikan.html` | 「あわせて読みたい」に追加 |
| `sitemap.xml` | `/kaitou-sokuhou/` を priority 0.9 で追加 |
| `_redirects` | `/kaitou-sokuhou.html` と `/sokuhou` → `/kaitou-sokuhou/` の301 |

### ページ構成
1. 予告ブロック（解答速報は試験当日15時以降に公開予定）
2. 令和8年度 試験日程表
3. 合格点（合格ライン）の推移：令和2〜令和7年度、受験者数/合格者数/合格率/合格点
4. 自己採点後の動き方（3ゾーン：38点以上／ボーダー±2点／30点以下）
5. 来年リベンジ → /courses/ 内部リンク＋四谷学院A8リンク（`data-aff="A8_YOTSUYA"`）
6. FAQ（FAQPage JSON-LD あり）／出典
7. BreadcrumbList JSON-LD

### SEO
- title：`宅建 解答速報 2026｜自己採点と合格ライン｜令和8年度（10月18日）| 宅建GYM`
- description：解答速報／自己採点／合格ライン／試験日・合格発表日を含む
- canonical：`https://takken.mainichi-lab.com/kaitou-sokuhou/`
- 構造化データ：FAQPage ＋ BreadcrumbList

### 広告表記
- PRブロック（`.pr-note`）を「来年リベンジすると決めたら」セクション冒頭に配置
- A8リンクは `rel="nofollow sponsored noopener"` ＋ `data-aff` ＋ インプレッションピクセル（www10）
- 新規リンクは作らず `affiliate_links.md` / `AFF_SLOTS.md` の既存 a8mat を流用

---

## 3. 掲載した事実と出典（絶対に推測で書き換えない）

### 令和8年度（2026年）日程 — 出典：一般財団法人不動産適正取引推進機構 https://www.retio.or.jp/exam/schedule/
| 項目 | 日程 |
|---|---|
| 官報公告 | 令和8年6月5日（金） |
| 申込（ネット） | 令和8年7月1日 9:30 〜 7月31日 23:59（受付終了） |
| 申込（郵送） | 令和8年7月1日 〜 7月15日（受付終了） |
| 受験票発送 | 令和8年10月2日（金）予定 |
| 試験日 | 令和8年10月18日（日）13:00〜15:00（登録講習修了者 13:10〜15:00） |
| 合格発表 | 令和8年11月25日（水） |
| 受験手数料 | 8,200円（都道府県公式案内で確認） |

### 合格点推移 — アガルート／STUDYing の公表集計が一致することを確認して掲載
| 年度 | 受験者 | 合格者 | 合格率 | 合格点 |
|---|---|---|---|---|
| 令和7（2025） | 245,462 | 45,821 | 18.7% | 33点 |
| 令和6（2024） | 241,436 | 44,992 | 18.6% | 37点 |
| 令和5（2023） | 233,276 | 40,025 | 17.2% | 36点 |
| 令和4（2022） | 226,048 | 38,525 | 17.0% | 36点 |
| 令和3（2021・10月） | 209,749 | 37,579 | 17.9% | 34点 |
| 令和3（2021・12月） | 24,965 | 3,892 | 15.6% | 34点 |
| 令和2（2020・10月） | 168,989 | 29,728 | 17.6% | 38点 |
| 令和2（2020・12月） | 35,261 | 4,610 | 13.1% | 36点 |

※ 令和7年度の公式PDF（goukaku.retio.or.jp）はCIDフォントでテキスト抽出不可のため、
数値は検索結果の公式発表引用＋2社の集計一致で確認。**公式PDFの原文照合は未実施**（要判断事項）。

---

## 4. 【最重要】試験当日（2026年10月18日）の差し替え手順

### 🔴 当日は「1回で終わる作業」ではない（2026-08-31 リハで判明）

各社の解答速報は**時間差で出る**。令和7年度（2025/10/19）の実績は次のとおり：

| 発表元 | 速報の公開時刻（前年実績） |
|---|---|
| ユーキャン | 15:15頃から順次 |
| TAC | 15:20から順次（16:10〜 合格推定点、18:00〜 全問解説のライブ） |
| 日建学院 | 16:15〜 解答速報会（YouTube） |
| LEC | **17:00から順次**（16:30〜 解答速報会ライブ） |

出典：[ユーキャン](https://www.u-can.co.jp/宅建/sokuhou/)／[モアライセンス](https://moalicense.jp/takken/gaiyou_sokuhou.htm)／[試験部](https://shikaku-pass.net/kaito-takken/)。**令和8年度の実際の時刻は当日に必ず実測すること**（上表は前年実績＝目安）。

つまり **15:30 の時点では複数社の突き合わせができない**。
自動タスクは 15:30 に1本しかないため、**15:30の回で `seitou.js` に正答を入れようとしてはいけない**（1社だけの速報を入れる＝後で番号が変わると、採点済みの受験者に嘘をついたことになる）。

### タイムライン（3パス構成）
| 時刻 | パス | やること |
|---|---|---|
| 〜10/17 | 前日 | 本ページを開き日程表が最新か確認。`python3 ~/Desktop/claude/tools/check_seitou.py` が通ることを確認。告知は **Threads @fujiken_ai**（🔴 X の @fujiken_ai は永久凍結。Xでの告知はしない） |
| 10/18 15:30 | **パス1（自動タスク）** | 予告ブロックを速報ハブに差し替え（下記A）＋速報リンク集の枠を作る（下記B）。**seitou.js は触らない**。デプロイして200を実測 |
| 10/18 18:30頃 | **パス2（要追加タスク）** | LEC・TAC・日建学院が出そろう。3社を突き合わせて `seitou.js` に正答を差し込む（下記B2）。ここが本番 |
| 10/18 21:00頃 | **パス3（要追加タスク）** | 割れていた問の決着＋各社の予想ボーダーを比較表に入れる（下記C）。Threads投稿 |
| 10/19〜10/25 | 追随 | 各社が予想を修正するので比較表と seitou.js を追随更新 |
| 11/25（合格発表日） | 確定 | 令和8年度の確定合格点・合格率を推移表に追記（下記D）。**seitou.js を機構の公式正解で照合し `status:"kakutei"` に**。ここが最大の流入日 |
| 11/25〜12月 | 収益 | リベンジ導線を強化（下記E）。ここが収益の本番 |

> 🔴 **未実施の宿題**：現在スケジュールタスクは 10/18 15:30 の1本だけ。**パス2（18:30）とパス3（21:00）のタスクが無い**。
> 司令塔が `mcp__scheduled-tasks__create_scheduled_task` で2本追加すること（内容は §8）。

### A0. タスクが発火しなかった場合（手動フォールバック）
スケジュールタスクは **Claudeアプリが起動していないと発火しない**。10/18は日曜で、アプリが落ちている可能性がある。
- **10/18 は朝のうちにMacとClaudeアプリを起動しておく**（前日リマインダーに含める）
- 15:45 になっても本番ページが予告版のままなら、手動で新セッションを立て、この RUNBOOK の §4-A → B → B2 を上から順に実行する。プロンプトは「`宅建GYM_公開用/KAITOU_SOKUHOU_RUNBOOK.md` の §4 を読んで当日差し替えを実行して」の1行でよい
- 判断基準：`curl -s https://takken.mainichi-lab.com/kaitou-sokuhou/ | grep -c '試験当日に公開予定'` が **1 を返したらまだ差し替わっていない**（0 なら差し替え済み）

### A. 予告ブロックの差し替え（パス1＝15:30・所要5分）
`kaitou-sokuhou/index.html` の `<div class="notice">` ブロックを丸ごと置換する。

置換後の骨子：
- 見出し：`令和8年度 宅建試験 解答速報（10月18日実施）`
- 本文：「本試験は本日15時に終了しました。各社の解答速報リンクと自己採点の手順は以下です」
- 注意書き：「速報は各社の予想解答であり、後日修正されることがあります。正解は実施機関の公式発表が基準です」
- 併せて `<p class="updated">` の日付を `2026年10月18日` に更新

### B. 解答速報リンク集の追加（当日夕方）
`<h2 id="line">` の直前に新セクションを挿入。
**注意：他社の解答一覧そのものを転載しない（著作権リスク）。リンクと一言コメントのみ。**

確認先（当日に実URLを取得してから貼る。事前に決め打ちしない）：
- 一般財団法人不動産適正取引推進機構（公式・正解番号）https://www.retio.or.jp/
- 各資格スクールの解答速報ページ（TAC／LEC／日建学院／アガルート／STUDYing 等）
- 提携中の A8 講座（四谷学院・資格スクエア等）が速報を出していれば **そこは `data-aff` 付きのアフィリリンクで貼る**（PR表記は既存の `.pr-note` の下に配置すること）

### B2. 自己採点ツールの正答データ差し込み（パス2＝18:30頃）
予告版から**登録不要の50問自動採点ツール**（`<h2 id="saiten">`）が実装済み。正答は `kaitou-sokuhou/seitou.js` の1箇所に分離してあり、**このファイルだけ編集すれば採点が有効になる**（index.html の採点ロジックは触らない）。

#### 正答の出典ポリシー（迷ったらここに戻る）
- **採用する社＝LEC・TAC・日建学院の3社**を基本にする（宅建で最も参照される3社。ユーキャン・アガルート・スタディングは補助）
- **3社が一致した問だけ `1〜4` を入れる。1社でも違う問は `0`（未確定）にする。** 「多数決で2対1だから2を入れる」は**やらない**。速報が割れる問は例年ほんとうに割れており、後で番号が変わる
- **1社しか出ていない段階で入れない。** 2社以上そろってから着手する（15:30の回では絶対にやらない）
- 転記は必ず**リンク先の各社ページを自分で開いて読む**。まとめサイト（モアライセンス等）の孫引きで入れない
- 疑わしいときは `0` にしておく。**未確定はページ側が正しく扱う**（下記の判定保留）。間違った番号を入れるほうが害が大きい

#### 手順
1. `window.TAKKEN_SEITOU_2026.answers` に問1〜問50の正答を **50要素ちょうど**で記入。上のポリシーどおり、一致した問だけ `1〜4`、割れている問・未確認の問は `0`
2. `status` を `"sokuhou"` に（11/25以降、機構の公式正解で照合したら `"kakutei"` に）
3. `source` に出典（例：`"LEC・TAC・日建学院の解答速報で一致した番号（2026/10/18 18:30時点）"`）、`updated` に日時を記入
4. index.html 内の `<script src="/kaitou-sokuhou/seitou.js?v=1">` の **`v=` を上げてキャッシュを飛ばす**（受験者は当日中に再訪する。旧キャッシュのまま「未公開」表示になるのを防ぐ）
5. 🔴 **push する前に必ず事前チェックを通す**：
   ```
   python3 ~/Desktop/claude/tools/check_seitou.py
   ```
   要素数違い・0〜4以外の値・`?v=` の上げ忘れ・転記ミスらしい偏りを検出する。**exit 1 のまま push しない**
   （※ このMacに `node` は入っていない＝2026-08-31実測。当日 node 前提のツールを書かないこと）
6. デプロイ後、本番で実測確認：
   ```
   curl -s "https://takken.mainichi-lab.com/kaitou-sokuhou/seitou.js?nocache=$(date +%s)" | head -20
   curl -s "https://takken.mainichi-lab.com/kaitou-sokuhou/?nocache=$(date +%s)" | grep -o 'seitou.js?v=[0-9]*'
   ```
   **2つめが古い `v=` を返したらHTMLがまだ配信されていない**。数分待って再確認する（Cloudflare Pages のビルド＋伝播に通常1〜3分）

#### 未確定問がある状態の採点結果（2026-08-31 リハで修正済み）
`0`（未確定）が1問でもあると、ページは**合否ゾーンを断定しない**：
- 確定分の得点を `lo`、未採点が全部正解だった場合を `hi` として、**`lo`〜`hi` のレンジ**を表示する
- `lo>=38`（確実にゾーン1）、`hi<=32`（確実にゾーン3）、`33<=lo && hi<=37`（確実にゾーン2）のときだけゾーンを断定する
- それ以外は **「判定保留：未採点の◯問しだいで結論が変わります」** を出し、リベンジ講座への導線は出さない

> **なぜこうしたか**：修正前は確定分の素点だけで判定していたため、
> 「38問確定・うち32点／未確定12問」の受験者に **「ゾーン3：33点未達／切り替えた人から翌年受かります」** と表示し、
> リベンジ講座を売りに行っていた。その人の実際の点は 32〜44点で、**合格している可能性が高い**。
> 当日夕方は未確定が出るのが通常状態なので、これは例外ではなく既定動作だった。**ここは絶対に戻さないこと。**

注意：ユーザーの入力は各自の端末の localStorage（キー `takkenSaiten2026`）にのみ保存され、サーバーには来ない。**当日15時前に入力して保存しておき、正答反映後にワンタップ採点する**流れを想定済み。したがって **`seitou.js` を更新しても受験者の入力は消えない**（キーを変えてはいけない）。

### C. 合格ライン予想の追加（10/19〜25）※比較表の枠は実装済み（2026-08-30）
`<h2 id="border">` に **LEC／TAC／日建学院／アガルート／ユーキャン／スタディングの6行の空枠**（全行「未発表」）を用意済み。新セクションを作らず、**この表（`tbody id="borderBody"`）の該当行を埋める**：
- 各社が公式に発表した数字のみ記入し、「発表状況・出典」列に発表元ページへのリンクを貼る
- **必ず「◯◯社の予想」と主語を明記**し、宅建GYMの断定にしない。出典なしの数字・憶測は書かない（未発表の行は「未発表」のまま残す）
- 各社が予想をレンジ（例：36±1）で出した場合はレンジのまま転記する

### D. 合格発表日（11/25）の更新 ← 収益上いちばん重要
1. 推移表の先頭に `令和8年度（2026）` の行を追加（受験者数・合格者数・合格率・合格点）。出典は実施機関の発表。
2. 予告/速報ブロックを「合格発表を受けて」の内容に差し替え。
3. **ゾーン別の文言を「発表後」向けに書き換える**：
   - 合格した人 → **`/blog/takken-goukakugo`（合格後の手続き：登録実務講習・資格登録37,000円・宅建士証4,500円・合格から1年の壁）へリンク**。2026-08-13に公開済みなので、当日は新規に書かずリンクを張るだけでよい
   - 不合格だった人 → **`/blog/takken-saichousen`（点数別の再挑戦戦略）を最上部に置き、そこから `/courses/` へ渡す**。2段構えにするのは、発表直後に比較ページを直撃させるより「まず何点足りなかったか」で受け止めたほうが離脱が少ないため
   - この2本は既に本ページの「あわせて読みたい」に入っている（11/25はゾーン本文側にも入れる）
4. `<p class="updated">` を更新、`sitemap.xml` の該当URLに `<lastmod>` を付けてもよい。
5. Search Console でURL検査 → インデックス登録をリクエスト。

### E. 11月末〜1月の運用
- 「宅建 不合格 来年」「宅建 リベンジ」系の検索意図に合わせ、ゾーン3セクションを独立記事に切り出すことを検討
- A8の審査通過（スタディング／アガルート／フォーサイト）が出ていれば /courses/ に昇格させ、本ページからのリンクも張り替える

### デプロイ手順（毎回共通）
```
python3 ~/Desktop/claude/tools/check_seitou.py      # ← seitou.js を触った回は必須。NGなら push しない
cd /Users/fujiken/Desktop/claude/宅建GYM_公開用
git add -A && git commit -m "解答速報: 試験当日の速報リンク集に差し替え" && git push
sleep 90
curl -sI "https://takken.mainichi-lab.com/kaitou-sokuhou/" | head -1                       # 200
curl -s "https://takken.mainichi-lab.com/kaitou-sokuhou/?nc=$(date +%s)" | grep -c '試験当日に公開予定'   # 差し替え済みなら 0
curl -s "https://takken.mainichi-lab.com/kaitou-sokuhou/?nc=$(date +%s)" | grep -o 'seitou.js?v=[0-9]*'  # 上げた番号が返るか
```
**上の3本が期待どおりになるまで「完了」と報告しない。** git push が通っただけでは本番に出ていない。
※ 2026-08-13に `takken.html`＋`build.sh` は廃止。**`index.html` を直接編集する**（詳細は README）。

---

## 5. 禁止事項（信頼を壊さないため）

- 出典のない日付・点数を書かない。「例年◯点だから今年も◯点」と断定しない
- 他社の解答一覧・問題文を転載しない（リンクのみ）
- 「合格ライン◯点確定」と合格発表前に書かない
- アフィリリンクを PR表記・`rel="nofollow sponsored"` なしで置かない

---

## 6. 未対応・要判断

- ~~令和7年度の公式PDFはテキスト抽出不可（CIDフォント）~~ → **2026-08-13に照合済み**。`https://goukaku.retio.or.jp/exam/pdf_2025_1_UWbaZCx6hm/2025result.pdf` は `pypdf` でテキスト抽出でき、機構の原文で 受験者245,462／合格者45,821／合格率18.7％ を確認した（平均年齢36.2歳・登録講習修了者の合格率24.2％も同PDF）。**11/25の令和8年度分も同じ方法で原文照合すること**（`pdftotext` は未インストール。`python3 -c "from pypdf import PdfReader"` を使う）
  - ただし**合否判定基準（33点）だけは同PDFに記載がない**。機構の合格発表ページ側にあるので、点数はそちらで確認する
- Search Console へのインデックス登録リクエストは未実施（手動作業が必要）
- ~~解答速報の「当日の実作業」を自動スケジュールタスク化するかは未決定~~ → **`takken-kaitou-sokuhou-2026` を 10/18 15:30 に登録済み**。ただし1本だけでは足りない（§4のタイムライン参照・パス2/3の追加が宿題）
- `_headers` の先頭コメントが `//` で書かれている（Cloudflare Pages の `_headers` のコメント記号は `#`）。現状 noindex は効いているようなので当日は触らない。**10/18 が終わってから**別途検証する

---

## 7. 当日の失敗モード一覧（2026-08-31 リハで洗い出し）

**年に一度の一発勝負。落とすと商戦最大のスパイクを丸ごと失う。** 各行の「検知」を実際に実行して確かめること。

| # | 失敗モード | 検知方法 | 対処 |
|---|---|---|---|
| 1 | **タスクが発火しない**（Claudeアプリ未起動・Mac スリープ） | 15:45に `curl -s https://takken.mainichi-lab.com/kaitou-sokuhou/ \| grep -c '試験当日に公開予定'` が 1 | §4-A0 の手動フォールバック。**前日にMac起動＋スリープ解除＋アプリ起動を確認しておく**のが本命の対策 |
| 2 | **15:30に正答を入れようとして失敗する／1社だけで入れてしまう** | パス1のログに `seitou.js` の編集が含まれている | パス1は `seitou.js` に触らない設計にした（§4タイムライン）。1社だけの番号を入れてしまったら即 `0` に戻して再デプロイ |
| 3 | **`answers` が50要素でなく採点が丸ごと無効**（ページは黙って「未公開」表示に落ちる＝当日誰も気づけない） | `python3 ~/Desktop/claude/tools/check_seitou.py` が exit 1／ブラウザのコンソールに `[seitou.js] answers の要素数が…` | チェッカーを push 前に必ず通す。ページ側も console.error を出すようにした |
| 4 | **`?v=` 上げ忘れ**で旧 seitou.js がキャッシュされ「未公開」のまま | チェッカーが検出／`curl ... \| grep -o 'seitou.js?v=[0-9]*'` が旧番号 | index.html の `v=` を上げて再デプロイ |
| 5 | **Cloudflare のビルド／伝播遅延**で push しても本番が変わらない | §4デプロイ手順の curl 3本 | 90秒待って再確認。5分たっても変わらなければ Cloudflare Pages のダッシュボードでビルド失敗を確認 |
| 6 | **未確定問があるのに合否ゾーンを断定**し、合格者にリベンジ講座を売る | リハで再現済み（§4-B2の囲み） | **修正済み**（判定保留レンジ表示）。ゾーン判定ロジックを元に戻さないこと |
| 7 | **速報の番号が後で変わり、採点済みの受験者に嘘をつく** | 各社ページを再確認 | 3社一致した問だけ入れる（§4-B2 出典ポリシー）。変わったら即 `0` に戻すか正しい番号に直し、`updated` を更新 |
| 8 | **アクセス集中で落ちる** | ー | 静的HTML＋Cloudflare Pages なので実質心配ない。**当日に重い機能を足さない**ことだけ守る（採点は全部クライアント側・サーバー通信ゼロ） |
| 9 | **他社の解答一覧を転載してしまう**（著作権） | 差分レビュー | リンクと一言コメントのみ。番号の一覧表をページに載せない（自己採点ツールの内部データとして持つのは可） |
| 10 | **凍結済みアカウントで告知しようとして時間を溶かす** | ー | 🔴 X の @fujiken_ai は永久凍結。**告知は Threads @fujiken_ai**。カード投資用の @kentarofujita_ は使わない |
| 11 | **node 前提のツールを書いて当日動かない** | ー | このMacに node は無い（2026-08-31実測）。当日書くスクリプトは **python3 のみ** |
| 12 | **受験者の入力が消える** | ー | localStorage キー `takkenSaiten2026` を変更しない。`seitou.js` の更新では消えない |

---

## 8. スケジュールタスクの追加宿題（司令塔へ）

現在 `takken-kaitou-sokuhou-2026`（10/18 15:30・one-time）の1本のみ。**下記2本の追加を推奨**（`mcp__scheduled-tasks__create_scheduled_task`）。

| taskId案 | 日時 | 内容 |
|---|---|---|
| `takken-sokuhou-pass2-20261018` | 2026/10/18 18:30 | LEC・TAC・日建学院の3社を突き合わせて `seitou.js` に正答を差し込む（RUNBOOK §4-B2）。**当日の本丸** |
| `takken-sokuhou-pass3-20261018` | 2026/10/18 21:00 | 割れていた問の決着＋各社の予想ボーダーを比較表に反映（§4-C）。Threads告知 |

前日リマインダー（10/17）も推奨：「Mac起動・Claudeアプリ起動・`check_seitou.py` の疎通確認・Threads告知1本」。
